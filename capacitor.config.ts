import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reliable.fieldengineer',
  appName: 'RELIABLE CODING',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
